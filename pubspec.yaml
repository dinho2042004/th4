name: th4
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 RSS + API
  http: ^1.2.0
  webfeed: ^0.7.0

  # 🔥 State (dark mode)
  provider: ^6.0.5

  # 🔥 Hiển thị web trong app
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true